# Webpack Starter Kit

## Installation
1. Clone the repository
2. Go to the project
3. Run `npm install`

## Running on development 

Run `npm run dev` 

## Build for production 

Run `npm run build` to build project.
